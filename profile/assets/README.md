# assets

Two images ship here, both referenced from `profile/README.md` by raw GitHub
URL — so keep the filenames stable when you replace them.

| File | Where | Job |
|---|---|---|
| `nebelhaus-banner-rounded.png` | top of the org profile | the identity shot |
| `hero.png` | below the intro | the S1 hero — the house, in use |

## The banner

The org profile lives or dies on this one image. It should carry, in one frame:

- **Pounce** open (the command palette — proves it's a real native app)
- **Nebelung** colours obvious across at least two apps (the silver-grey identity)
- **SketchyBar** perched at the top edge
- **AeroSpace** tiling visible (a couple of tiled windows)

Aim for a clean desktop, a real workflow on screen (not an empty terminal), and
retina resolution. To swap it, drop the new render in over the same filename.

## The hero

`hero.png` is **S1** from the workshop's
[`assets/SHOTLIST.md`](https://github.com/nebelhaus/workshop/blob/main/assets/SHOTLIST.md),
and this file is a byte-for-byte copy of `nebelhaus/assets/hero.png` in the rice,
which is the canonical one. Reshoot there, then copy it here — otherwise the org
page and the rice README drift into two different desktops.

It sits below the intro paragraph with alt text describing tiled windows, the
sill bar, and the nebelung palette. Keep that alt text truthful if you reshoot.
